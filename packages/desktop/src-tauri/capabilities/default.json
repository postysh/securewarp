{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Capabilities enabled on the main SecureWarp window. Phase 3 adds opener (external links → system browser). Future phases can layer on fs + shell when the web app calls native IPC for reveal-in-Finder, dock badge, etc.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
