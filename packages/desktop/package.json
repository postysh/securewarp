{
  "name": "@securewarp/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "SecureWarp desktop app — Tauri shell around the web dashboard UI.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1"
  }
}
